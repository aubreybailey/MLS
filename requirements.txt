# Pinned baseline — exact versions verified working together.
# Dev cycle: keep these pinned for reproducible builds; to advance, bump the
# versions in a dedicated commit, rebuild, test, and re-pin (or revert).
# Last pinned: 2026-07-18

# Core
homeharvest==0.8.18
pandas==2.3.3
geopandas==1.1.4
shapely==2.1.2
requests==2.34.2

# Mapping
folium==0.20.0

# Web UI
streamlit==1.59.2
streamlit-folium==0.27.2
streamlit-searchbox==0.1.24
